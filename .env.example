# Mock is opt-in for tests and demos only. Omit this setting for normal real-API use.
# MJPS_LLM_MODE=mock
MJPS_RESPONSE_STORAGE=normal
# OPENAI_API_KEY is read from the environment or OS key store when available.
